{
  "name": "packer-builder-pi",
  "version": "0.0.1",
  "description": "Packer & Pi-Gen pipeline for building Pi images",
  "main": "index.ts",
  "scripts": {
    "prepare": "husky install",
    "build:pre": "git clone --branch arm64 git@github.com:RPi-Distro/pi-gen.git tmp/pi-gen",
    "build:ci": "npm run build:pre && make images",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/luvly-luvs/packer-builder-pi.git"
  },
  "author": "",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/luvly-luvs/packer-builder-pi/issues"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
